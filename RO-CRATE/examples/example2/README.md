# embedded column metadata

a ro-crate with embedded column metadata as suggested by gemini

validation in roc-validator fails with:

- [Violation]: The 3 occurrences of the JSON-LD key "measurementMethod" are not allowed in the compacted format because it is not present in the @context of the document
- [Violation]: The 3 occurrences of the JSON-LD key "valueUrl" are not allowed in the compacted format because it is not present in the @context of the document
